[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "docorganizer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pymupdf>=1.24",
    "watchdog>=4.0",
    "pyyaml>=6.0",
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
ocr = [
    "pytesseract>=0.3",
    "Pillow>=10.0",
]

[project.scripts]
docorg = "docorg.__main__:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["docorg*"]
